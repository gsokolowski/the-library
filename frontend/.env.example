# Laravel GraphQL origin for the Vite dev proxy (adjust port/path for XAMPP, artisan serve, etc.)
VITE_BACKEND_URL=http://127.0.0.1:8000

# Optional: absolute GraphQL URL for production or when not using the proxy (e.g. https://api.example.com/graphql)
# VITE_GRAPHQL_URL=
